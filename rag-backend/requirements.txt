# ── Web Framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
starlette>=0.40.0

# ── LLM Providers ────────────────────────────────────────────────────────────
groq>=1.0.0
ollama>=0.3.0

# ── Embeddings / ML ──────────────────────────────────────────────────────────
sentence-transformers>=3.0.0
torch>=2.2.0
transformers>=4.40.0
huggingface_hub>=0.23.0
tokenizers>=0.19.0
safetensors>=0.4.0
scikit-learn>=1.4.0
numpy>=1.26.0

# ── Vector Stores ─────────────────────────────────────────────────────────────
qdrant-client>=1.9.0
lancedb>=0.10.0
chromadb>=0.5.0

# ── PDF Ingestion ────────────────────────────────────────────────────────────
PyMuPDF>=1.24.0           # import fitz
pdfplumber>=0.11.0
pytesseract>=0.3.10       # image_captioner.py

# ── Text Processing / Retrieval ───────────────────────────────────────────────
langchain-text-splitters>=0.2.0
rank-bm25>=0.2.2

# ── Supabase (Auth + Storage) ─────────────────────────────────────────────────
# Pinned to <2.16.0: supabase 2.16+ pulls storage3>=2.26 which requires pyiceberg,
# and pyiceberg needs Microsoft C++ Build Tools to compile on Windows.
# supabase 2.15.x uses storage3<0.12 (old version scheme) which has no such dep.
supabase>=2.3.0,<2.16.0

# ── Auth / JWT ───────────────────────────────────────────────────────────────
PyJWT[cryptography]>=2.8.0
bcrypt>=4.0.0

# ── Settings / Config ────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
httpx>=0.27.0
requests>=2.32.0

# ── Data / Export ─────────────────────────────────────────────────────────────
pandas>=2.2.0
pyarrow>=15.0.0           # lancedb_store.py

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.0

# ── Async / Utilities ────────────────────────────────────────────────────────
anyio>=4.4.0
tenacity>=8.3.0
pillow>=10.3.0            # image_captioner.py
python-dateutil>=2.9.0

# ── Email validation (pydantic EmailStr) ─────────────────────────────────────
email-validator>=2.2.0